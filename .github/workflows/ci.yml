name: Test Environment

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v5

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '22'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Generate Gradle Wrapper
        working-directory: Week6_Gradle
        run: gradle wrapper

      - name: Grant execute permission
        working-directory: Week6_Gradle
        run: chmod +x gradlew

      - name: Run Integration Tests
        working-directory: Week6_Gradle
        run: gradle clean test --tests "com.example.Selenium.tests.OrderTestIT"

      - name: Restore Allure History
        if: always()
        uses: actions/download-artifact@v4
        with:
          name: allure-history
          path: Week6_Gradle/build/allure-results/history
        continue-on-error: true

      - name: Generate Allure Report
        if: always()
        uses: simple-elf/allure-report-action@master
        with:
          allure_results: Week6_Gradle/build/allure-results
          allure_report: Week6_Gradle/build/allure-report
          keep_reports: 20

      - name: Upload Allure Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: Week6_Gradle/build/allure-report

      - name: Save Allure History
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-history
          path: Week6_Gradle/build/allure-report/history